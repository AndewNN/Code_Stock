#include<bits/stdc++.h>
using namespace std;
stack<char> st;
char a[1000];
int main()
{
    int n,i,j,len,ch;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf(" %s",a);
        ch = 1;
        len = strlen(a);
        if(a == '('){

        }
    }
    return 0;
}
